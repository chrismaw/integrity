<nav class="d-none d-md-flex navbar navbar-expand-md navbar-dark py-0">
    {% if settings.header_logo %}
        <a class="navbar-brand" href="{{ app_uri }}">
            <img src="{{ app_uri }}img/{{ settings.header_logo }}"/>
        </a>
    {% endif %}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link text-white h4" href="{{ app_uri }}admin/settings">Settings</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white h4" href="{{ app_uri }}admin/sections">Sections</a>
            </li>
        </ul>
    </div>
</nav>